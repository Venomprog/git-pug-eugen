extends ../../layout/layout

block variables
  -
    const pageName = 'main'
    const pageTitle = 'Main'

block content
  main.container(data-barba='container', data-barba-namespace=pageName)
    +main-hero
